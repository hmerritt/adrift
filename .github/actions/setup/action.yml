name: Setup
description: Setup Bun and install dependencies

runs:
  using: "composite"
  steps:
    - uses: oven-sh/setup-bun@v2

    - name: Install dependencies
      shell: bash
      run: |
        bun install
